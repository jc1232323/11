│         功能         │                        现状                        │               建议               │
├──────────────────────┼────────────────────────────────────────────────────┼──────────────────────────────────┤
│ 忘记密码             │ 完全没有，AboutPage 里写着"请妥善保管"             │ 接入邮件服务，做密码重置流程     │
├──────────────────────┼────────────────────────────────────────────────────┼──────────────────────────────────┤
│ 邮箱验证             │ 注册后直接登录，不验证邮箱真实性                   │ 发送验证链接，未验证限制 AI 使用 │
├──────────────────────┼────────────────────────────────────────────────────┼──────────────────────────────────┤
│ AI 对话额度管控      │ 后端有 DAILY_CHAT_LIMIT 环境变量但代码没有执行限制 │ 需要真正计数+前端显示剩余次数    │
├──────────────────────┼────────────────────────────────────────────────────┼──────────────────────────────────┤
│ LLM 出错时的用户体验 │ 如果 DeepSeek API 超时/报错，没有优雅降级          │ 加重试机制和友好错误提示         │
├──────────────────────┼──────────────────────────────────────────────────────────┤
1、现在等登录页面没有“忘记密码“的功能这会让用户很难去找回自己的密码。帮我添加一个”忘记密码“的功能。接入邮件服务，做密码重置流程
2、也没有”邮箱验证“的功能，这会导致一些网站的安全隐患。帮我在注册页面上添加一个邮箱验证的功能。发送验证链接，未验证限制 AI 使用
3、还有一个bug帮我修复。LLM 出错时的用户体验。如果 DeepSeek API 超时/报错，没有优雅降级。加重试机制和友好错误提示 
┌──────────────┬──────────────────────────────┬────────────────────────────────────────────┐
│     功能     │             现状             │                    建议                    │
├──────────────┼──────────────────────────────┼────────────────────────────────────────────┤
│ 知识点搜索   │ 只能通过目录树浏览，没有搜索 │ 加关键词搜索入口                           │
├──────────────┼──────────────────────────────┼────────────────────────────────────────────┤
│ 学习进度追踪 │ 用户不知道自己学了多少       │ 显示已学习的知识点、做题数量、连续学习天数 │
├──────────────┼──────────────────────────────┼────────────────────────────────────────────┤
│ 收藏/错题本  │ 没有                         │ 允许收藏知识点和标记做错的题               │
├──────────────┼──────────────────────────────┼────────────────────────────────────────────┤
│ 训练题后端化 │ 所有题目硬编码在前端 JS 里   │ 迁移到数据库，方便增删改+统计              │
├──────────────┼──────────────────────────────┼────────────────────────────────────────────┤
│ 对话导出     │ 不能导出聊天记录             │ 支持导出为 PDF/Markdown                    │
└──────────────┴──────────────────────────────┴────────────────────────────────────────────┘
1、现在知识点相对来说比较多，用户很容易找不到，只能通过目录树浏览，没有搜索。帮我添加一个“知识点搜索”功能，加关键词搜索入口。
2、现在用户不能查看他的学习进度，导致用户不知道学了多少。添加一个“学习进度跟踪”功能。显示已学习的知识点、做题数量、连续学习天数
3、现在用户没有一个地方去收藏他的错题或者是知识点。帮我添加一个“收藏本”功能允许收藏知识点和标记做错的题
4、训练题后端化，所有题目硬编码在前端 JS 里迁移到数据库，方便增删改+统计
5、用户现在没有地方可以导出对话帮我添加一个新功能“导出对话”。支持导出为 PDF/Markdown。